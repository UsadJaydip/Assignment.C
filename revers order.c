#include<stdio.h>
#include<string.h>
 int main()
{
    char str[40]; 
    printf (" \n eter a string to b reversed :");
    scanf (" %s", str);
    
  
    printf(" \n after the reverse of a string: %s ", strrev(str));
    
    
}
